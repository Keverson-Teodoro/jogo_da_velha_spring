spring:
  application:
    name: Jogo da velha
  datasource:
    url: jdbc:postgresql://localhost:5432/jogo_da_velha
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect